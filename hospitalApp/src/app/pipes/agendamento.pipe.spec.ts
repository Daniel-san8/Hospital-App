import { AgendamentoPipe } from './agendamento.pipe';

describe('AgendamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new AgendamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
